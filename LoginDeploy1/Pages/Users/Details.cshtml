@page
@model LoginDeploy1.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>User</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.User.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.User.name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.User.pass)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.User.pass)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.User.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
